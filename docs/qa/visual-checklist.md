# Visual QA Checklist

- Download page screenshot shows left navigation, URL input, options, teal primary button, preview area, and Chinese labels.
- Formats page screenshot shows resolution, container, bitrate, actual format, and subtitle controls without text overlap.
- Queue page screenshot shows title, status, progress, speed, ETA, and actions.
- Queue task cards show video thumbnails when the analyzed thumbnail is available.
- History page screenshot shows search and history table.
- Settings page screenshot shows save folder, cookies.txt, ffmpeg, concurrency, and update controls.
- About page screenshot shows app version, yt-dlp status, ffmpeg state, and third-party license entry.
- UI follows `docs/gui-reference.png`: light Win11 surfaces, teal primary action, compact utility layout, and left navigation.
- Left navigation uses icon+Chinese-label rail items without ellipsis, duplicate native icons, or scrollbar artifacts.

## Current QA Evidence

- Full pytest: pass on current working tree.
- Compile check: pass on current working tree.
- `git diff --check`: pass on current working tree, with only the existing CRLF notice for `docs/qa/visual-diff.json`.
- Real URL analysis smoke uses `https://www.youtube.com/watch?v=PqQNXB6hhUs`.
- Real download matrix completed audio+video `18`, audio-only `251`, and video-only `160`; each case wrote history and downloaded a real file under `.qa-real-smoke/matrix/20260603-010026`.
- Real batch smoke completed 3 URLs including a Shorts link and a short video with concurrency 2 under `.qa-real-smoke/batch/20260603-040245`, with history output paths matched to each record title.
- Real subtitle smoke completed subtitle-file download and ffmpeg burn-in on the Shorts link under `.qa-real-smoke/subtitles/20260603-010200`.
- Real full GUI flow smoke completed analysis, preview URL extraction, download, queue, and history under `.qa-real-smoke/full/20260603-013630`.
- Current screenshots are saved under `docs/qa/screenshots/`; the primary Download screenshot uses a deterministic audio+video visual sample.
- `scripts/render_qa_screenshots.py` now validates every saved screenshot size, including History, Settings, and About, and separates visual fixture content from real network/download smoke tests.
- Current packaging checkpoint generated `dist/YTDL-GUI/YTDL-GUI.exe`, confirmed bundled `yt-dlp.exe --version` printed `2026.03.17`, confirmed bundled `ffmpeg.exe -version` printed `8.1-essentials_build-www.gyan.dev`, and confirmed the executable opened a responding window titled `视频地址提取器`.

## Visual Status

- Current UI has been moved toward the selected reference: custom title bar, compact icon+label left rail, bottom yt-dlp status bar, light Win11 surfaces, teal primary actions, staged download flow, reference-style audio/video/preview option rows, thumbnail preview overlay and duration badge, reference-style format resolution rows, thumbnail queue cards, queue-side recent history, and completed status badges.
- Strict visual comparison is generated by `scripts/visual_compare.py` into `docs/qa/visual-diff.json`; the script now rejects screenshot size mismatches instead of silently scaling actual screenshots.
- Latest strict comparison: Download exact `1.0557%`, Formats exact `1.7575%`, Queue exact `1.9881%`.
- Pixel-perfect equality with `docs/gui-reference.png` remains unproven and is not claimed here.
